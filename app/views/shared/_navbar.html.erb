<header>
  <nav>
    <% if user_signed_in? %>
      <div class="flex gap-x-2 justify-end pr-2">
        <p class="text-sm text-slate-200">Hi <%= current_user.name %></p>
        <%= link_to "Log out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete } %>
      </div>
    <% else %>
      <div class="flex gap-x-2 justify-end pr-2">
        <%= link_to "Log in", new_user_session_path, class: "self-right" %>
        <%= link_to "sign in", new_user_registration_path, class: "self-right" %>
      </div>
    <% end %>
  </nav>
</header>